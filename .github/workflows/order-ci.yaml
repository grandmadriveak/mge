# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: Order services cicd

on:
  pull_request:
    branches: [ "main" ]
    paths:
      - "Common/**"
      - "Microservices/Order/**"
      - ".github/workflows/order-ci.yaml"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x
    - name: Restore dependencies
      run: dotnet restore Microservices/Order/Order.API
    - name: Build
      run: dotnet build Microservices/Order/Order.API --no-restore
    - name: Test
      run: dotnet test Microservices/Order/Order.API --no-build --verbosity normal

  deploy:
    needs: [build]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
